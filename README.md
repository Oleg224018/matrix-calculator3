# Калькулятор матриц Тощев ИС-323 💾

> Консольное приложение на Go для выполнения базовых операций с матрицами 2×2 и 3×3.

---

## 🎯 Цель работы

Реализовать консольное приложение для счета матриц 2×2 и 3×3 с использованием:
- ✅ Сложение матриц
- ✅ Умножение матрицы на число
- ✅ Умножение 2х матриц друг на друга

---

## 📌 Функционал

- ✅ **Сложение матриц** — складывает две матрицы одинакового размера
- ✅ **Умножение матрицы на число** — умножает все элементы матрицы на заданное число
- ✅ **Умножение двух матриц** — перемножает две матрицы (только 2×2 или 3×3)
- ✅ **Интерактивное меню** — команды: `1-4` для удобного выбора операции
- ✅ **Ввод размера матрицы** — поддержка 2×2 и 3×3
- ✅ **Проверка ошибок** — например, несовпадение размеров матриц

---

## 🧩 Структура проекта

```
matrix-calculator3/
├── main.go          # Основной файл с меню, логикой ввода/вывода и всеми функциями
├── go.mod           # Управление зависимостями Go
└── README.md        # Документация проекта
```

---

## ⚙️ Как запустить

1. Убедитесь, что установлен **Go 1.20+**  
   Скачать: https://go.dev/dl/

2. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/Oleg224018/matrix-calculator3.git
   cd matrix-calculator3
   ```

3. Запустите программу:
   ```bash
   go run main.go
   ```

---

## 🖥 Пример работы

```
=== Калькулятор матриц Тощев ИС-323 💾 ===

Выберите операцию:
1. Сложение матриц
2. Умножение матрицы на число
3. Умножение двух матриц
4. Выход
Ваш выбор: 1

Введите размер матрицы (2 или 3): 2

Первая матрица (2x2):
Элемент [1][1]: 1
Элемент [1][2]: 2
Элемент [2][1]: 3
Элемент [2][2]: 4

Вторая матрица (2x2):
Элемент [1][1]: 5
Элемент [1][2]: 6
Элемент [2][1]: 7
Элемент [2][2]: 8

Результат сложения:
    6.00     8.00 
   10.00    12.00 

Ваш выбор: 4
До свидания!
```

---

## 🧾 Описание кода (анализ по строкам)

---

### 🔹 `main.go`

#### package main
```go
package main
```
Первые строки кода импортируют **главный пакет** `main`. Это **точка входа** в программу.

#### import (
```go
import (
	"errors"
	"fmt"
	"os"
)
```
Далее происходит импорт компонентов:
- `errors` — для создания кастомных ошибок
- `fmt` — для ввода и вывода
- `os` — для завершения программы

---

### 🔹 Функции для операций с матрицами

#### func AddMatrices(a, b [][]float64) ([][]float64, error)
```go
func AddMatrices(a, b [][]float64) ([][]float64, error) {
	if len(a) != len(b) || len(a[0]) != len(b[0]) {
		return nil, errors.New("матрицы должны быть одинакового размера")
	}

	size := len(a)
	result := make([][]float64, size)
	for i := range result {
		result[i] = make([]float64, size)
		for j := range result[i] {
			result[i][j] = a[i][j] + b[i][j]
		}
	}
	return result, nil
}
```
Функция для **сложения двух матриц**:
- Проверяет, что размеры матриц совпадают
- Создаёт новую матрицу `result`
- Проходит по каждому элементу и складывает значения: `a[i][j] + b[i][j]`
- Возвращает результат или ошибку

#### func MultiplyMatrixByScalar(matrix [][]float64, scalar float64) [][]float64
```go
func MultiplyMatrixByScalar(matrix [][]float64, scalar float64) [][]float64 {
	size := len(matrix)
	result := make([][]float64, size)
	for i := range result {
		result[i] = make([]float64, size)
		for j := range result[i] {
			result[i][j] = matrix[i][j] * scalar
		}
	}
	return result
}
```
Функция для **умножения матрицы на число**:
- Создаёт новую матрицу `result`
- Проходит по каждому элементу и умножает на `scalar`

#### func MultiplyMatrices(a, b [][]float64) ([][]float64, error)
```go
func MultiplyMatrices(a, b [][]float64) ([][]float64, error) {
	size := len(a)
	if len(a) != len(b) || len(a[0]) != len(b[0]) {
		return nil, errors.New("матрицы должны быть одинакового размера")
	}

	result := make([][]float64, size)
	for i := range result {
		result[i] = make([]float64, size)
		for j := range result[i] {
			for k := 0; k < size; k++ {
				result[i][j] += a[i][k] * b[k][j]
			}
		}
	}
	return result, nil
}
```
Функция для **умножения двух матриц**:
- Проверяет, что размеры совпадают
- Реализует **алгоритм умножения матриц**:
  - `result[i][j] += a[i][k] * b[k][j]` — суммирует произведения
- Возвращает результат или ошибку

---

### 🔹 Функции ввода и вывода

#### func printMatrix(matrix [][]float64)
```go
func printMatrix(matrix [][]float64) {
	size := len(matrix)
	for i := 0; i < size; i++ {
		for j := 0; j < size; j++ {
			fmt.Printf("%8.2f ", matrix[i][j])
		}
		fmt.Println()
	}
}
```
Функция для **вывода матрицы** в консоль:
- Двойной цикл проходит по строкам и столбцам
- Печатает значения с форматированием `"%8.2f"` (2 знака после запятой)

#### func inputMatrix(size int, name string) [][]float64
```go
func inputMatrix(size int, name string) [][]float64 {
	fmt.Printf("\n%s (%dx%d):\n", name, size, size)
	matrix := make([][]float64, size)
	for i := range matrix {
		matrix[i] = make([]float64, size)
		for j := range matrix[i] {
			fmt.Printf("Элемент [%d][%d]: ", i+1, j+1)
			var val float64
			fmt.Scanln(&val)
			matrix[i][j] = val
		}
	}
	return matrix
}
```
Функция для **ввода матрицы**:
- Принимает `size` (размер матрицы) и `name` (имя матрицы)
- Возвращает матрицу типа `[][]float64`
- Запрашивает у пользователя элементы матрицы по индексам `[i][j]`

#### func getMatrixSize() int
```go
func getMatrixSize() int {
	var size int
	for {
		fmt.Print("Введите размер матрицы (2 или 3): ")
		fmt.Scanln(&size)
		if size == 2 || size == 3 {
			break
		}
		fmt.Println("Неверный размер. Введите 2 или 3.")
	}
	return size
}
```
Функция для **запроса размера матрицы**:
- Проверяет, что введено 2 или 3
- Повторяет запрос, если введено другое число

---

### 🔹 Обработчики операций

#### func handleMatrixAddition()
```go
func handleMatrixAddition() {
	size := getMatrixSize()
	a := inputMatrix(size, "Первая матрица")
	b := inputMatrix(size, "Вторая матрица")

	result, err := AddMatrices(a, b)
	if err != nil {
		fmt.Printf("Ошибка: %s\n", err)
		return
	}

	fmt.Println("\nРезультат сложения:")
	printMatrix(result)
}
```
Функция для **обработки сложения матриц**:
- Сначала запрашивает размер матрицы
- Затем вводит обе матрицы
- Вызывает функцию `AddMatrices`
- Выводит результат или ошибку

#### func handleScalarMultiplication()
```go
func handleScalarMultiplication() {
	size := getMatrixSize()
	matrix := inputMatrix(size, "Матрица")
	var scalar float64
	fmt.Print("Введите число для умножения: ")
	fmt.Scanln(&scalar)

	result := MultiplyMatrixByScalar(matrix, scalar)
	fmt.Println("\nРезультат умножения на число:")
	printMatrix(result)
}
```
Функция для **обработки умножения матрицы на число**:
- Запрашивает размер матрицы
- Вводит матрицу
- Вводит число (скаляр)
- Вызывает `MultiplyMatrixByScalar`
- Выводит результат

#### func handleMatrixMultiplication()
```go
func handleMatrixMultiplication() {
	fmt.Println("Умножение матриц: A * B")
	fmt.Print("Размер первой матрицы (2 или 3): ")
	var sizeA int
	fmt.Scanln(&sizeA)

	fmt.Print("Размер второй матрицы (2 или 3): ")
	var sizeB int
	fmt.Scanln(&sizeB)

	if sizeA != sizeB {
		fmt.Println("Ошибка: размеры матриц должны совпадать для умножения 2x2 или 3x3.")
		return
	}

	a := inputMatrix(sizeA, "Первая матрица")
	b := inputMatrix(sizeB, "Вторая матрица")

	result, err := MultiplyMatrices(a, b)
	if err != nil {
		fmt.Printf("Ошибка: %s\n", err)
		return
	}

	fmt.Println("\nРезультат умножения:")
	printMatrix(result)
}
```
Функция для **обработки умножения двух матриц**:
- Запрашивает размеры обеих матриц
- Проверяет, что размеры совпадают
- Вводит обе матрицы
- Вызывает `MultiplyMatrices`
- Выводит результат или ошибку

---

### 🔹 func main()
```go
func main() {
	fmt.Println("=== Калькулятор матриц Тощев ИС-323 💾 ===")
	fmt.Println()

	for {
		fmt.Println("Выберите операцию:")
		fmt.Println("1. Сложение матриц")
		fmt.Println("2. Умножение матрицы на число")
		fmt.Println("3. Умножение двух матриц")
		fmt.Println("4. Выход")

		var choice string
		fmt.Print("Ваш выбор: ")
		fmt.Scanln(&choice)

		switch choice {
		case "1":
			handleMatrixAddition()
		case "2":
			handleScalarMultiplication()
		case "3":
			handleMatrixMultiplication()
		case "4":
			fmt.Println("До свидания!")
			os.Exit(0)
		default:
			fmt.Println("Неверный выбор. Попробуйте снова.")
		}
		fmt.Println()
	}
}
```
Основная функция:
- Выводит заголовок и меню
- В бесконечном цикле:
  - Выводит список команд
  - Считывает выбор пользователя
  - Вызывает соответствующую функцию обработки
  - При выборе `4` — завершает программу


